import "styled-components";

import { Theme } from "@mui/material/styles";

interface CustomTheme {
  $styled: {
    borderRadius: {
      sm: string;
    };
    colors: {
      blue_shed: string;
    };
  };
}
